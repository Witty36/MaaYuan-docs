diff --git a/es/components/common/article-page/src/components/outline.mjs b/es/components/common/article-page/src/components/outline.mjs
index 4ee813c4585faa10e24ebfd42d7cde7545c2edcb..1df44fda42da34b9e7ff7113247c023ce7698d61 100644
--- a/es/components/common/article-page/src/components/outline.mjs
+++ b/es/components/common/article-page/src/components/outline.mjs
@@ -1,4 +1,3 @@
-import { getScrollOffset } from 'vitepress';
 import { onMounted, onUpdated, onUnmounted } from 'vue';
 import { useNamespace } from '../../../../../composables/use-namespace.mjs';
 import 'vitepress-theme-teek/theme-chalk/tk-copy-banner.css';
@@ -66,7 +65,8 @@ const useActiveAnchor = (container, marker) => {
     const isBottom = Math.abs(scrollY + innerHeight - offsetHeight) < 1;
     const headers = resolvedHeaders.map(({ element, link }) => ({
       link,
-      top: getAbsoluteTop(element)
+      top: getAbsoluteTop(element),
+      scrollMarginTop: Number.parseFloat(getComputedStyle(element).scrollMarginTop) || 0
     })).filter(({ top }) => !Number.isNaN(top)).sort((a, b) => a.top - b.top);
     if (!headers.length) {
       activateLink(null);
@@ -81,8 +81,8 @@ const useActiveAnchor = (container, marker) => {
       return;
     }
     let activeLink = null;
-    for (const { link, top } of headers) {
-      if (top > scrollY + getScrollOffset() + 4) break;
+    for (const { link, top, scrollMarginTop } of headers) {
+      if (top > scrollY + scrollMarginTop + 4) break;
       activeLink = link;
     }
     activateLink(activeLink);
